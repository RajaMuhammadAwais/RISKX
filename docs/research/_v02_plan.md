# V0.2 IMPLEMENTATION PLAN (working file)

Repo: /home/ubuntu/RISKX, module github.com/RajaMuhammadAwais/RISKX, go 1.26.5.
Env: export PATH=$PATH:/usr/local/go/bin:/home/ubuntu/go/bin. staticcheck in ~/go/bin. golangci-lint v2.1.6 (scripts/setup_env.sh).

Research done, docs: docs/research/v0.2-reporting-cicd-cloud.md (final, has refs), schemas/sarif-schema-2.1.0.json downloaded (official errata01 schema). ADR-0008 written (storage wiring).

## Files to create
1. `internal/reporting/reporting.go` — package reporting: Reporter interface (ReportSummary(in SummaryInput) ([]byte,error) etc), SummaryInput{Assets,Findings,Scores}, section struct (executive_summary/risk_overview/critical_exposures/affected_assets/evidence/remediation), sections deferred: identity/cloud/agent/mcp marked "future" in output. Human text report + JSON report.
2. `internal/reporting/jsonl.go` — JSONLWriter: writes metadata line (version+attribution) + one canonical JSON per record (findings first, then scores). RFC-compliant.
3. `internal/reporting/csv.go` — CSVWriter: RFC 4180 (encoding/csv), flattens id/asset/asset_kind/title/severity/confidence/status/validation/references(references joined ";"), evidence NOT flattened (documented projection limitation).
4. `internal/reporting/sarif.go` — SARIFExporter: models.Finding→sarif.result{ruleId=f.ID, level (critical→error, high→error, medium→warning, low→note), message.text=Title+" | "+Observation, locations [{physicalLocation{artifactLocation{uri=f.AssetValue}}}], properties{riskx_confidence,riskx_status,riskx_model_version,riskx_evidence_count,riskx_references}, fingerprints{RISKX-ContentID=f.ID}}; run.tool.driver.name=RISKX, version=0.2.0; log{version:"2.1.0",runs:[{tool,results}]}. NO region (no source code).
5. `internal/cloud/aws/sigv4.go` — SigV4 with empty payload; Sign(req, accessKey, secretKey, region, service, query string) error.
6. `internal/cloud/aws/aws.go` — Client{Region,AKID,Secret} from env (AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY, AWS_REGION default us-east-1). Methods: Whoami(ctx), Instances(ctx) ([]Instance), Buckets(ctx) ([]Bucket), Identities(ctx) ([]IAMIdentity) — all XML parse with encoding/xml into typed structs. MaxItems pagination for IAM (IsTruncated loop, MaxItems=1000), S3 unpaginated OK per quota (note limitation). EC2 DescribeInstances MaxResults=1000 + NextToken loop.
7. `internal/cloud/aws/aws_test.go` — parse tests with recorded XML fixtures; sign test vectors (AWS test vectors from sigv4 docs: GET request example, date 2015-08-30, region us-east-1, service iam, expected signature f4780e2d9f65fa890f1c3c95d0a2497ad170635fe81502083e2e4421394b60ae — verify carefully or compute locally and assert against stored expected).
8. `internal/validate/validate.go` — package: VerifyDNS(ctx, host, expectedRecordType+value) (resolve via net.Resolver, compare), VerifyTLS(ctx, host) (tls.Dial, x509 verify against system roots, report subject/SANs/expiry/self-signed), VerifyHTTP(ctx, url) (GET 5s timeout, status, server header, banner snippet truncated). Each returns ValidationResult{Checked,Passed,Observed,Evidence}.
9. `cmd/riskx/store.go` — helper: openStore() using resolveDataPath (flag --data → env RISKX_DATA → default ~/.riskx/riskx.db), mkdirAll 0700, open, ensure 0600 check; storeClose with error collect. `--no-data` disables.
10. `internal/validate/validate_test.go`, `internal/cloud/aws/fixtures/` XML.
11. `.github/workflows/ci.yml`, `lint.yml`, `staticcheck.yml`, `vuln.yml`
12. `.golangci.yml` (v2 syntax; enable govet, staticcheck, unused, errcheck, gosec where clean)
13. `docs/research/benchmarks.md` (Q.4): KEV parse benchmark (existing BenchmarkKEVParse), graph benchmarks, + new discovery benchmarks. Run benches: go test -bench . -benchtime=1s ./... record n/op, ns/op, MB/s.
14. Update README version 0.2.0, todo.md, _progress.md.

## Files to edit
- cmd/riskx/helpers.go: add resolveDataPath(openStore)
- cmd/riskx/cmd_discovery.go: discover → PutAssets before emit; assets list → ListAssets from store; vuln → PutFindings+PutEvidence; risk → read stored assets (ListAssets) to score each stored asset + PutRiskScore
- cmd/riskx/cmd_analysis.go: report summary → build from storage (assets+findings+scores) with --data flag; export jsonl/csv/sarif → wire to reporting pkg with --output flag (default stdout, file ok); validate → replace stub with real VerifyDNS/TLS/HTTP actions + store validated evidence; graph list/centrality stay demo (graph persistence Phase 11); policy check keep stub w/ note.
- pkg/models: maybe add SchemaReport/etc constants. Add evidence preset AWSCloudDiscovery to internal/evidence/evidence.go.
- internal/storage/storage.go: add ListEvidence(findingID) and maybe CountEvidence.
- cmd/riskx/main.go: nothing (command registration unchanged).
- cmd/riskx/cmd_discovery.go add --data flag to discover/vuln/risk/assets; cmd_analysis.go add --data to report/export/validate.
- README.md: version 0.2.0; add storage/report/export/cloud/validate sections; versions table (storage-v1, reporting-v1, graph-v1, risk-v1, asset-v1, finding-v1, evidence-v1, cloud-v1?).

## Model facts needed
- models: AssetKind consts include KindHost,KindIP,KindDomain,KindService,KindApplication,KindAPI,KindCloudResource,KindContainer,KindAgent,KindMCPServer,KindIdentity. ExposureInternet/ExposureInternal/ExposureUnknown. Severity consts SevCritical/High/Medium/Low (string). Confidence consts ConfidenceHigh etc. StatusObserved/Inferred/Potential/Validated. ValidationUnvalidated/Pending/Validated/Failed. Suppression{Reason,Owner,CreatedAt,ExpiresAt,Expired,IsActive(now)}. Finding fields: ID,Schema,AssetID,AssetValue,Title,Description,Observation,Evidence,Severity,Confidence,Status,Validation,Classification,Remediation,Suppression,References,CreatedAt. Evidence{Type,Source,Timestamp,Value,Citation(SourceCitation{Org,Doc,URL,Accessed})}. RiskScore{AssetID,Score,Severity,Factors[RiskFactor{Name,Weight,Value,Evidence,Citation}],Weights,ModelVersion,Stale,Incomplete}. idgen.AssetID(kind,value,host,port) and idgen.FindingID(asset,cve).
- Storage Store API: Open(path string) (*Store,error) — NOTE: check if Open creates dir (probably not; mkdir in helpers). PutAssets([]Asset)(int,error); PutFindings([]Finding)error; PutEvidence([]Evidence,findingID,assetID)error; PutRiskScore(RiskScore)error; PutRelationships; ListAssets/ListFindings/ListRiskScores; Count(a,f,e). schemaVersion "storage-v1".
- ingest: NewKEVClient(), NewNVDClient(key), NewEPSSClient(), NewOSVClient(); normalize.Sources{KEV,NVD,EPSS,OSV}; normalize.Fuse(ctx,sources,id)(Vulnerability,error); findings.NewGenerator(sources); gen.CVEFinding(ctx,asset,cveID)(Finding,[]Evidence,error). Risk: risk.NewEngine(nil), eng.Score(risk.InputBundle) InputBundle{AssetID,Exposure,Criticality,KEV bool,EPSS,ScoreAvailable,MaxCVSS,Centrality,HighPrivilege,StandardsGap,Evidence,StaleInputs}.
- policy: policy.Evaluate(policy, findings, scores); exit codes 0/1/2.
- mode: mode.Parse(s) (passive|safe|active|validation); mode.SecurityMode interface String(); mode.NewAuthorizer(w io.Writer, r io.Reader, ci bool, preapprove bool); auth.Require(m, actions []mode.Action) error; mode.Action{Description,Target,Method}; mode.DefaultAuthorizer(ci,preapprove).
- output: output.NewMeta(modeStr), output.AddNVDAttribution(&meta), printer(cmd).EmitJSON(Result{Meta,Payload}).
- errs: errs.Input(code,msg,hint), errs.New(code,name,msg); CodeModeDenied const.
- evidence preset sources: evidence.CISAKEV, NVDAPI, FIRSTEPSS, OSVAPI, OWASPMCP, MCPSpec, OWASPTop10, CISControls, MITREATTACK — add AWSCloudDiscovery = Source{Org:"AWS", Doc:"AWS API Reference (STS/EC2/S3/IAM)", URL:"https://docs.aws.amazon.com/", Accessed:"2026-08-12"}.
- graph: graph.New(), AddNode(Node{ID,Label,Kind,Score}), AddEdge(Edge{ID=graph.EdgeID(from,to,typ),From,To,Type,Status,Weight,Evidence}); g.CentralityReport(mode); graph.ModeEvidenceBacked; graph.ModelVersion; graph.NewFromAssets(assets,rels,scores) maybe not exist.
- KEV fixture tests/fixtures/kev.csv; env RISKX_SKIP_FEEDS=1 for live gates; tests use testing.Short() + short flag.

## CLI contract reminders
- Exit codes: 0 ok, 1 policy violation, 2 execution error. Modes: passive default. No guessing: insufficient confidence when no evidence. NVD attribution string in vuln outputs. 0600 DB, 0700 dir. --json, --quiet, --verbose global flags.

## Test/bench plan
- reporting: golden human report, JSONL roundtrip parse, CSV parse RFC, SARIF validate against downloaded schema (use go-jsonschema or simple unmarshal + manual required-field checks — prefer using github.com/santhosh-tekuri/jsonschema/v6 for schema validation in tests).
- aws: fixture XML parse tests, SigV4 test vectors, env-absent explicit error.
- validate: localhost/DNS real checks gated testing.Short; unit with custom resolver via test helpers (net.Resolver override) — VerifyDNS takes Resolver param for testability.
- Benchmarks.md with actual run output.

## SARIF schema validation dep decision
- Add test dependency go-jsonschema (santhosh-tekuri/jsonschema/v6) used ONLY in tests (test helper file with +build or separate internal/reporting/sarif_test.go importing jsonschema). Actually put schema validation in sarif_test.go to keep runtime dep-free. 

## SigV4 test vector (AWS docs canonical GET example)
GET https://iam.amazonaws.com/?Action=ListUsers&Version=2010-05-08
host: iam.amazonaws.com
x-amz-date: 20150830T123600Z
date 20150830, region us-east-1, service iam. Expected SignatureToSign: 20150830T123600Z / 20150830/us-east-1/iam/aws4_request / 3511de7e9fa38e73904c0c38632256779c6a0704d850effe38fcd939248d9219 / 09e46e39ac7138ae598284c4e83e3e24b5c908c5e16c9adbcc1fac7c3313485b (string-to-sign + hashed payload verified from AWS docs). I will instead compute locally in test and assert against hard-coded expected values derived from the canonical spec steps (preimage, credential scope, string-to-sign, signature) — compute expected values once via reference implementation in test setup OR hardcode known AWS published values. SAFER: hardcode the AWS-published expected StringToSign and expected Signature:
- Credential scope: 20150830/us-east-1/iam/aws4_request
- String to sign: AWS4-HMAC-SHA256\n20150830T123600Z\n20150830/us-east-1/iam/aws4_request\n09e46e39ac7138ae598284c4e83e3e24b5c908c5e16c9adbcc1fac7c3313485b
- Expected signature (AWS secret key example w/ AKIDAKID.../wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY): f4780e2d9f65fa890f1c3c95d0a2497ad170635fe81502083e2e4421394b60ae

## Cloud cmd design
`riskx cloud aws <action>` where action in whoami|instances|buckets|identities|all. Whoami needs no IAM perms. All require creds env; absent → errs output exit 2 explicit. Mode must be safe+ (validation allowed too). Authorization plan printed then Require().

## STATUS SNAPSHOT (pre-implementation, 2026-08-12)
- internal/reporting/reporting.go WRITTEN: Summary(in SummaryInput{Assets,Findings,Scores})→JSONSummary{Schema:"report-v1",Model:"reporting-v1",GeneratedAt,Sections[Section{Title,Content}],Counts{Assets,Findings,CriticalFindings,HighFindings,ActiveSuppressions,SuppressedFindings,AverageRiskScore,MaxRiskScore,FutureSections}}; future sections listed from out-of-MVP: attack path/identity/cloud/AI agent/MCP. HumanReport(sum) string with NVDAttribution footer. MarshalJSONCanonical. Uses models.SevCritical/SevHigh; Suppression.IsActive(now).
- reporting TODO next: internal/reporting/jsonl.go (JSONLWriter w/ metadata header line), csv.go (CSVWriter RFC4180), sarif.go (SARIF 2.1.0 exporter) + sarif_test.go (validate vs docs/research/schemas/sarif-schema-2.1.0.json using santhosh-tekuri/jsonschema/v6).
- Models API recap: Finding fields ID,Schema,AssetID,AssetValue,Title,Description,Observation,Evidence[]Evidence,Severity(Severity string const SevCritical|SevHigh|SevMedium|SevLow),Confidence,Status,Validation,Classification,Remediation(*Remediation{Problem,WhyItMatters,Evidence,Fix,Verification,Rollback}),Suppression(*Suppression{Reason,Owner,CreatedAt,ExpiresAt,Expired,IsActive(now)}),References[]string,CreatedAt. Severity type Severity string? check constants: pkg/models/models.go line 179-ish "type Severity" — const SevCritical models.SevCritical. Evidence{Type,Source,Timestamp,Value,Citation SourceCitation{Organization,Document,URL,Accessed,Version}}. output.NVDAttribution exists in internal/core/output (verify name; fallback: define own attribution string "This product uses the NVD API but is not endorsed or certified by the NVD.").
- Remaining todo.md items v0.2: 6.1/6.2/6.4/6.5, Q.2 (CI), Q.4 (benchmarks), Q.5 partially.

## SARIF schema VERIFIED facts (from official errata01 schema at docs/research/schemas/sarif-schema-2.1.0.json, 2026-08-12)
Top-level: required ["version","runs"]; version type string (no enum); runs type ["array","null"] minItems 0. run: required ["tool"]; results prop exists. result: required ["message"] only; level enum ["none","note","warning","error"] default "warning"; fingerprints type object additionalProperties string; properties is $ref propertyBag (additionalProperties: true, also has tags/keys optional props — fine with just extra keys); locations array; ruleId string. tool driver: toolComponent has name+version props (check names 'name','version' exist — yes standard). run.tool.driver.rules exists in toolComponent props.
CONFIRMED our sarif.go structs are schema-compliant. Fixes needed: none structural. level default warning; we emit error/note/warning per severity — compliant.
Tests: sarif_test.go must validate emitted JSON against schema via santhosh-tekuri/jsonschema/v6 (compile with go get in tests only).

## SARIF test debugging state (2026-08-12, ongoing)
Problem: jsonschema/v6 (v6.0.3) Compile() validates every schema resource against its metaschema, and even a trivial {} or {"$schema":"draft-04 url"} fails with "got string, want object" at '' in BOTH draft-04 and draft-2020-12 metaschemas. Even case {} against 2020-12 metaschema fails. This suggests the library's metaschema bundled in v6.0.3 may be broken OR the Go env issue (we run in sandbox with go 1.26.5). Test command used: compiler := jsonschema.NewCompiler(); compiler.AddResource("http://test", tmpFile); compiler.Compile("http://test"). Repro file: docs/research/schemas/repro.go.
Hypotheses left: (1) v6.0.3 is broken — try older v6.0.1 or use v5 (github.com/santhosh-tekuri/jsonschema/v5 which is stable). (2) The metaschema validation requires internet? No, bundled. DECISION PLAN: switch to jsonschema/v5 in sarif_test.go (v5 has Compiler without metaschema-strict compile? v5 also validates metaschema but known-stable). Alternatively validate SARIF output structurally in Go (custom required-fields checker) AND run jsonschema validation only if it works. Simplest proven path: use v5.
Reporting test failures remaining: TestSummaryEmptyInputs (Section title mismatch: our empty-inputs section content for Critical exposures says "No critical-severity findings are open" but test checks "No critical exposures recorded in the evidence store" — fix test to match implementation wording), TestCSVScoresAndAssets (line 4 wrong fields: Scores then Assets written consecutively; assets header has 9 cols, scores rows have 6 — reader sees row with wrong field count; fix: write all scores then all assets is fine but CSV reader reads whole buf sequentially; issue: after w.Flush in Scores, then Assets writes header row "id,kind,..." — that's fine; wrong count suggests something else: maybe sampleScores has extra rows or the suppression column count. Check: rows[1] is first score row -> 6 fields; rows[2] is second score (6); rows[3] assets header (9) → reader error "record on line 4 wrong number of fields" (line 4 = rows index 3). FIX: emit both sections in separate buffers OR document mixed-section CSV is not supported and test accordingly. Choose: separate writers per section (cleaner); CSVWriter stays single-section.
sarif_test.go also still needs DefaultDraft(jsonschema.Draft4) fix applied (it was edited in but compile still fails with same error, so fixing lib first).
IMPORTANT: after fixing, run `go test -count=1 -race ./internal/reporting/` + full suite `go test -race ./...` + `go build ./...` + `go vet ./...` before committing.
Remaining v0.2 work after reporting tests pass: (1) cmd wiring store.go helper + discover/vuln/risk/assets persistence (todo.md items), (2) real validate command (internal/validate DNS/TLS/HTTP), (3) AWS cloud discovery (internal/cloud/aws SigV4 + client; test vectors in plan file above), (4) .github/workflows ci/lint/staticcheck/vuln + .golangci.yml, (5) benchmarks.md, (6) README update to 0.2.0 + ADR index entry ADR-0008, (7) commit+push, tag v0.2.0.

## STATE 2026-08-12 (post reporting completion)
DONE: internal/reporting (reporting.go, jsonl.go, csv.go, sarif.go + tests incl schema validation vs jsonschema/v5) ALL PASS. helpers.go: resolveDataPath(f,RISKX_DATA,off,~/.riskx/riskx.db)/openStore/storeClose added. cmd_discovery.go: discover --data (writes assets via putAssets); assets list --data (ListAssets); vuln --data (PutFindings + PutEvidence per f.Evidence); risk --data (scores stored assets via assetBundle + PutRiskScore; falls back to documented demo with warning "no evidence store; scoring an explicit demo input" in meta.Warnings).
TO VERIFY BEFORE COMMIT: (1) output.Meta has Warnings field? check pkg/models or internal/core/output — I used meta.Warnings = append(meta.Warnings, ...) — VERIFY field name/type exists. (2) models.Asset has Criticality string field? VERIFY — if not, remove that block or store it. (3) strconv imported in cmd_discovery.go? It was imported via helpers import only — VERIFY imports in cmd_discovery.go (add strconv if missing). (4) storage SchemaVersion() exported — yes line 141 func SchemaVersion() string.
REMAINING v0.2: internal/validate (DNS/TLS/HTTP real checks + cmd wiring replace stub in cmd_analysis.go); internal/cloud/aws (sigv4.go client SigV4 + Query APIs STS/EC2/S3/IAM XML; fixtures; cmd wiring cloud); .github/workflows ci.yml lint.yml staticcheck.yml vuln.yml + .golangci.yml; benchmarks.md (run benches KEV+graph+new); README 0.2.0 update + ToolVersion in internal/core/config/config.go line ~22-27 is "0.1.0" → bump; todo/progress updates; commit+push; tag v0.2.0.
cmd_analysis.go has: report summary stub (build from storage with --data), export jsonl/csv/sarif stubs (wire reporting pkg --output default stdout), validate stub (replace with real VerifyDNS/TLS/HTTP + store), graph list/centrality stays demo w/ note. Cloud cmd stub exists in cmd_analysis.go too.
validate design: package internal/validate: VerifyDNS(ctx, host, typ, wantValue) ValidationResult{Checked,Passed,Observed,Evidence}; VerifyTLS(ctx, host) same; VerifyHTTP(ctx, url) same. cmd validate action subcmds: dns|tls|http. Test: use custom net.Resolver injection where possible; localhost gates testing.Short.
AWS design: internal/cloud/aws/sigv4.go Sign(accessKey,secret,region,service,method,uri,query string, body []byte, now time.Time) (string,string,string error) → returns auth header, date header, x-amz-content-sha256. Client FromEnv() checks AWS_ACCESS_KEY_ID+AWS_SECRET_ACCESS_KEY else explicit error. Region env AWS_REGION default us-east-1. Methods Whoami, Instances (EC2 DescribeInstances XML), Buckets (S3 ListBuckets XML), Identities (IAM ListUsers XML, paginated MaxItems=1000/Marker loop). Mode: safe+; cmd prints action plan then mode.Require. Fixtures: recorded XML in internal/cloud/aws/fixtures/.
CI: ci.yml (name CI; on push/PR; jobs test(linux go 1.26 matrix maybe linux+darwin): setup-go@v7 pinned, go build/vet/test -race, go mod tidy -diff); lint.yml golangci-lint-action@v9 pinned + .golangci.yml v2 syntax (run: go 1.26, linters enable govet,staticcheck,unused,errcheck; disable-all true); staticcheck.yml staticcheck-action@v1 pinned; vuln.yml govulncheck-action@v1 pinned. golangci-lint installed v2.1.6 in repo scripts/setup_env.sh — pin that version in lint.yml.
Benchmarks: existing: internal/vulnerability/ingest BenchmarkKEVParse, internal/risk, internal/graph benches; record with methodology in docs/research/benchmarks.md (hardware: sandbox ubuntu, go 1.26.5). Run: go test -bench . -benchtime=2s ./internal/vulnerability/ingest ./internal/risk ./internal/graph ./internal/reporting.
README: version table add reporting-v1, sarif-2.1.0, storage-v1, cloud-v1, validate-v1; sections for report/export/storage/cloud/validate; keep attribution string "This product uses the NVD API but is not endorsed or certified by the NVD."

## STATE 2026-08-12 (evening)
DONE so far (all compiling/testing): reporting pkg (Summary/ExportJSONL/ExportCSV/ExportSARIF + tests), cmd wiring discover/vuln/risk/assets persistence (--data flag), validate pkg (VerifyDNS/TLS/HTTP + tests) + cmd_analysis validate/report/expand wired, cloud/aws/sigv4.go Sign() + NewSignedRequest() + aws.go client (ConfigFromEnv, Whoami/Instances/Buckets/Identities, XML parse, ToAssets mapping KindCloudResource/KindIdentity) WRITTEN but NOT YET BUILT (has compile issues likely: url.Query() construction uses unimported "net/url" — imported yes; "errs.CodeInput" usage may be wrong name — check internal/core/errs codes; "models.ContentID" used as function, earlier facts said idgen.AssetID wraps models.ContentID — check signature; models.KindCloudResource/KindIdentity may not exist — check pkg/models Asset kinds; apiVersion("s3")=2006-03-01 for query API is fine).
NEXT IMMEDIATELY: 1) go build ./internal/cloud/aws, fix compile errors (likely: errs.CodeInput name, models.ContentID usage, missing Kind consts). 2) aws_test.go: sigv4 test vector (AWS canonical: 2015-08-30, GET https://iam.amazonaws.com/?Action=ListUsers&Version=2010-05-08, date 20150830T123600Z, region us-east-1, service iam, secret wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY → expected signature f4780e2d9f65fa890f1c3c95d0a2497ad170635fe81502083e2e4421394b60ae; expected string-to-sign: "AWS4-HMAC-SHA256\n20150830T123600Z\n20150830/us-east-1/iam/aws4_request\n09e46e39ac7138ae598284c4e83e3e24b5c908c5e16c9adbcc1fac7c3313485b"); fixture XML parse tests (fixtures/*.xml hand-written small). 3) cmd_analysis cloud discover cmd wire: actions whoami|instances|buckets|identities|all; replace stub; mode safe/validation; prints auth plan → auth.Require. 4) CI: .github/workflows/{ci,lint,staticcheck,vuln}.yml + .golangci.yml; lint action pinned v9 (uses golangci-lint v2.1.6 via args). 5) benchmarks.md + README bump 0.2.0 + config ToolVersion 0.2.0 + commit+push+tag v0.2.0.
NOTE: github action versions verified earlier: actions/setup-go@v7, golangci/golangci-lint-action@v9, dominikh/staticcheck-action@v1, golang/govulncheck-action@v1. golangci-lint binary v2.1.6 available in repo scripts/setup_env.sh (installs to ~/go/bin).
Evidence preset: add AWSCloudDiscovery to internal/evidence/evidence.go (Org AWS, Doc "AWS API Reference (STS/EC2/S3/IAM)", URL https://docs.aws.amazon.com/, Accessed 2026-08-12). Use in cloud cmd evidence items.
Cloud cmd existing stub: cmd_analysis.go newCloudCmd (discover, config-audit stubs) — replace discover with real.

## STATE 2 (2026-08-12, later)
cloud/aws DONE: sigv4.go (Sign/NewSignedRequest, now passes botocore cross-check) + aws.go client (ConfigFromEnv/Whoami/Instances/Buckets/Identities/XML parse/ToAssets) + aws_test.go (TestSigV4AgainstAWSSDK passes; botocore v1.43.62 reference at docs/research/schemas/_ref_sigv4.py; expected auth signature dad145687cde2dbf9684236b386711320b5997e4d31b3b5efe762858f46cc755; NOTE: hand-memorized "f4780e2d" example values from internet DO NOT match botocore — use only botocore-derived values). TestClientSendsSignedRequest/TestClientAPIError use httptest server + client.query(host+"/",...) — need to rerun.
REMAINING: 1) rerun cloud tests after context import fix. 2) wire cloud discover cmd in cmd_analysis.go newCloudCmd (replace stub; --data flag; whoami|instances|buckets|identities|all; mode safe/validation; auth.Require plan). 3) evidence preset: internal/evidence/evidence.go add AWSCloudDiscovery source (Org AWS, Doc "AWS API Reference", URL https://docs.aws.amazon.com/, Accessed 2026-08-12) — used in cloud cmd evidence. 4) benchmarks.md update. 5) README 0.2.0 + ToolVersion bump (internal/core/config or cmd root). 6) CI workflows .github/workflows/ci.yml (setup-go@v7, test -race linux/darwin/windows, vet, go mod tidy -diff), lint.yml (golangci/golangci-lint-action@v9, args: --version v2.1.6), staticcheck.yml (dominikh/staticcheck-action@v1), vuln.yml (golang/govulncheck-action@v1). 7) commit+push branch v0.2 + merge to main + tag v0.2.0, then deliver.
Repo: RajaMuhammadAwais/RISKX, main branch, push to main directly (initial push was direct to main).

## STATE 3 (evening, 2026-08-12)
DONE: cloud discover cmd wired in cmd_analysis.go (whoami/instances/buckets/identities/all; --action/--mode/--preapprove/--ci/--data; expandActions; PutAssets persistence; attribution append). All 14 test suites pass w/ race. CI workflows written: .github/workflows/{ci,lint,staticcheck,vuln}.yml (Go 1.25; setup-go@v7; golangci-lint-action@v9 version v2.1.6; staticcheck-action@v1; govulncheck-action@v1). go.mod go directive = 1.25.0 (modernc.org/libc requires 1.25; go get downgrade timed out/killed; kept 1.25 honest). .golangci.yml go version 1.25, existing linters (errcheck govet ineffassign staticcheck unparam unused whitespace).
STATICCHECK ISSUES TO FIX NOW:
1. internal/validate/validate.go:147: self-assignment "start = start" (SA4018) — remove/fix.
2. internal/reporting/reporting.go:62: var futureSections unused (U1000) — remove.
3. internal/validate/validate_test.go: staticResolver type + methods unused (U1000) — delete or use. (Test used newResolver? check; if tests pass with it unused, staticcheck flags) — likely because tests import custom resolver via validate.DefaultResolver? Investigate: maybe tests pass a resolver option Validate now uses DefaultResolver globally; if staticResolver unused, tests don't exercise it — check and use it (inject resolver option) or remove.
Local golangci-lint (built go1.24) cannot load go1.25 module — CI binary handles lint; do NOT rely on local lint.
NEXT: fix staticcheck issues -> run tests again -> update README (v0.2.0, new commands report summary/export jsonl/csv/sarif, cloud discover, validate real checks, CI), benchmarks.md (KEV snapshot, SARIF schema validation, SigV4 botocore cross-check), docs/research/_progress.md update, ADR-0009 CI?, commit+push, tag v0.2.0, deliver.

## STATE 4 (2026-08-12 evening)
FIXED: staticcheck issues (validate self-assign start; unused futureSections→populated Counts.FutureSections in Summary; staticResolver now wired via Resolver.static/LookupTable + lookupStatic(ctx,...); TestVerifyDNSStaticResolver added; dnsResult fixed: empty records never auto-pass). All 14 suites pass w/ race. Staticcheck ./... clean.
STILL IN PROGRESS — storage persistence bugs found in smoke test (discover→report pipeline):
1. FIXED: storeClose() called BEFORE use in discover, vuln, validate persistence blocks → moved storeClose AFTER use (cmd_discovery.go lines ~79-87, ~284-292; cmd_analysis.go ~390-398).
2. FIXED: findings/references column name clashes w/ SQLite reserved keyword → renamed SQL column "references"→"refs" in schema + PutFindings + ListFindings (storage.go).
3. FIXED: ListAssets & ListFindings Scan time.Time from TEXT col → scan sql.NullString + time.Parse(RFC3339) (done in ListAssets and ListFindings).
4. TODO CHECK: ListRiskScores scan — computed_at missing from scan? line ~323 scans 8 cols but riskscores table has computed_at (9 cols incl schema). Check ListRiskScores query/selects + RiskScore.ComputedAt field. If broken, fix scan similarly.
5. After fixes: rebuild, smoke test: ./riskx discover example.com --data /tmp/riskx-demo.db → persisted N assets (stderr), report summary shows counts>0, export sarif valid (version+runs).
6. Then: full go test -race ./..., update README + benchmarks.md + _progress.md, commit all as v0.2.0, push to RajaMuhammadAwais/RISKX, create tag v0.2.0. CI workflows ready (.github/workflows ci/lint/staticcheck/vuln.yml, Go 1.25). README should mention: CI actions pinned versions verified 2026-08-12; Go module 1.25.0; local golangci-lint needs >=1.25-built binary (CI uses action binary).

## STATE 5 — README update in progress (2026-08-12)
README.md edits: first two edits APPLIED (features table extended with report/export/validate/cloud/storage-v1 rows; quick-start updated to Go 1.25+ with persist/report/validate/cloud blocks). Edit 3 (Development→add CI/CD table + docs/adr 9 ADRs) FAILED — the heredoc code block in edit 3's "find" text didn't match (likely indentation/backticks). FIX: retry edit 3 with exact text: "```\ngo build ./...          # compile everything\ngo vet ./...            # static analysis\ngo test ./... -short    # unit + fixture tests (live feeds skipped)\ngo test ./...           # includes live feed integration tests\nCGO_ENABLED=1 go test ./... -race   # race detector (requires gcc)\ngo test -bench=. -run=^$ ./internal/risk ./internal/vulnerability/ingest\n```" (check README.md line ~103 actual indentation first; CGO_ENABLED comment line exists; replace CGO line with plain go test -race).
AFTER README: (1) write ADR-0008 storage wiring + ADR-0009 CI pinning (check docs/adr/ numbering - ADR-0007 was AWS, so next are 0008, 0009). (2) update todo.md checkboxes for 6.1/6.2/Phase-7-start/etc. (3) benchmarks.md DONE. (4) git add -A, commit "v0.2.0: reporting, SARIF export, storage persistence, cloud discovery, safe validation, CI/CD", push to RajaMuhammadAwais/RISKX main, tag v0.2.0, deliver.
Repo: /home/ubuntu/RISKX. Env: export PATH=$PATH:/usr/local/go/bin:/home/ubuntu/go/bin
