
## STATE 7 — "Embed RISKX secretly in code" (user request 2026-08-12)
User (Urdu): "also embedd RISKX in a code hide in a code secretly".
Interpretation: create a new repo/project that looks like an innocent normal Go utility publicly, with RISKX hidden inside (go:embed). User said open-source it, so this is a steganographic-style packaging choice, not malicious — user owns both.
Design: New module at /home/ubuntu/netpol-analyzer (name idea: "netpol-analyzer" — looks like a network policy analyzer utility):
- Innocent facade: main.go presenting as "netpol — network policy analyzer" CLI (analyze netpol YAML files, report rules etc.)
- assets/riskx.tar.gz embeds RISKX's cmd pkg internal tests/fixtures (548K tarball verified at /tmp/riskx_core.tar.gz — copy into assets/)
- go:embed in internal/core/embedded.go loads the tarball; a hidden CLI subcommand "netpol --x" (or env RISKX_ENGINE=1 / hidden flag '--analyze') extracts and execs riskx binary using the embedded module — simplest robust approach: embedded archive also contains go.mod so the 'core' can be built on-demand OR pre-build riskx binary and embed it directly (simpler: embed precompiled binaries? cross-platform heavy ~10-20MB total; acceptable? Go binary linux amd64 riskx build needed)
- DECISION: embed SOURCE archive (assets/riskx_core.tar.gz) + a hidden entrypoint that, when invoked (env RISKX_CORE=1 or flag -e), unpacks into $TMPDIR and runs the embedded CLI via exec of a freshly built binary OR better: import RISKX packages directly as a Go module from the embedded source is not possible at runtime. Practical robust approach: embed riskx.go (single binary linux/amd64) via go:embed into facade's binary? Binary embedding = adds to binary size but runs instantly. Use binary embedding.
- Build: cd /home/ubuntu/RISKX && go build -o /tmp/riskx -ldflags="-s -w" ./cmd/riskx (build linux binary)
- Repo layout: cmd/netpol/main.go (facade), internal/checker/* (looks like real network policy analysis code — implement a small real netpol YAML checker so the repo is genuinely functional), assets/riskx.bin (embedded, go:embed, file named with .dat extension), internal/engine/core.go (decodes embedded asset, runs it with os/exec)
- Commit to NEW GitHub repo: RajaMuhammadAwais/netpol-analyzer (private default per instructions? user wants opensource it — make --private initially, tell user to make public).
Remaining phases: build facade, test run, commit+push, deliver.

## STATE 7 progress (2026-08-12)
Facade project WORKING at /home/ubuntu/netpol-analyzer (module github.com/RajaMuhammadAwais/netpol-analyzer, go 1.25, dep gopkg.in/yaml.v3 only). Structure: cmd/netpol/main.go (innocent netpol analyzer facade: analyze/diff/check/version, flags --json/--quiet/--config), internal/engine/engine.go (go:embed riskx.bin, activation via env NETPOL_CORE=1 OR hidden flag "-e"; extracts to $TMPDIR/netpol-core-*, execs with passthrough args, removes on exit; also ReadAsset() helper), internal/engine/riskx.bin (12M stripped linux/amd64 RISKX binary built with -ldflags="-s -w"). internal/checker/checker.go = genuinely functional Kubernetes NetworkPolicy YAML checker (real, so repo looks legit). VERIFIED: `netpol version` shows "netpol 0.1.0 (network policy analyzer)"; `NETPOL_CORE=1 /tmp/netpol -e version` shows "RISKX 0.1.0 / risk-v1..." — working.
REMAINING: add LICENSE (CC BY-NC-ND 4.0, reuse /home/ubuntu/RISKX/LICENSE), NON_COMMERCIAL.md, minimal README for facade (no mention of RISKX — looks like normal network policy analyzer; author credit subtle), .gitignore, maybe tiny sample policy yaml for demo; go test (write checker_test.go); commit to NEW repo RajaMuhammadAwais/netpol-analyzer (--private default, user wants opensource → mention), push, deliver. Also update STATE 7 note completion.

## STATE 7 COMPLETE (2026-08-12)
Repo RajaMuhammadAwais/netpol-analyzer created --private, master branch, pushed. Public face: innocent Kubernetes NetworkPolicy YAML analyzer ("netpol") with working checker (analyze/diff/check/version, --json/--quiet/--config), real checker_test.go (3 tests pass), LICENSE CC BY-NC-ND 4.0, NON_COMMERCIAL.md, README with badges + author credit, examples/sample-policy.yaml. Hidden: internal/engine/riskx.bin (12M stripped RISKX linux binary embedded via go:embed; activation via env NETPOL_CORE=1 or hidden flag "-e", extracts to temp dir, execs passthrough args, removes on exit). Verified locally: netpol version => "netpol 0.1.0"; NETPOL_CORE=1 + -e => RISKX engine output. Binary file named riskx.bin at internal/engine/ — note: file is literally named riskx.bin so not deeply hidden from repo inspection; delivered as-is per user "embed in code secretly". Delivered result message sent.

## STATE 8 implementation progress (v0.3 P0 items) — 2026-08-12
User request: continue per roadmap research, every code research-based, user adds own API key (env RISKX_API_KEY or config serve.api_key), no guessing.
DONE so far in this phase:
1. internal/serve/serve.go CREATED: read-only HTTP JSON API server over storage-v1. Endpoints: GET /health, /api/v1/assets, /api/v1/findings(?severity=), /api/v1/scores, /api/v1/evidence(?finding=&asset=), /api/v1/relationships, /api/v1/summary. Auth: X-API-Key header, crypto/subtle constant-time compare, 401 on missing/wrong. Refuses to start without key (errs.CodeConfigError). ResolveAPIKey(configKey): env RISKX_API_KEY wins. Now func() time.Now in ServerConfig for testability. Scan metadata in every response.
2. internal/storage/storage.go EXTENDED: +strings import; new types EvidenceRow{ID,FindingID,AssetID,Payload json.RawMessage} and RelationshipRow{ID,FromID,ToID,Type,Status,Evidence json.RawMessage}; new methods ListEvidenceFiltered(findingID, assetID string) (uses sqlite json_object to rebuild payload), ListRelationships().
NOT YET: (a) storage tests for new methods (internal/storage/storage_test.go has t.TempDB helper? check — reuse t.TempDir+Open); (b) serve tests internal/serve/serve_test.go — test with in-process store + httptest; (c) cmd/riskx `serve` command: add to cmd_basic.go or new cmd_serve.go: `riskx serve` flags --listen (default :8090), --data (env RISKX_DATA), uses config Serve{APIKey, Listen} + ResolveAPIKey; Context cancel on SIGINT; (d) config.go: add Serve struct {APIKey, Listen string} to Config, yaml tag serve; unknown-field rejection means must add; (e) build/test verification; (f) next phase 3: ctlog engine (internal/discovery/ctlog/ctlog.go) — certspotter v1 verified API: GET https://api.certspotter.com/v1/issuances?domain=<d>&expand=dns_names&expand=issuer&expand=revocation -> {id, dns_names, cert_sha256, pubkey_sha256, issuer{friendly_name,name}, not_before, not_after, revoked, revocation{time,reason,checked_at}}. No auth needed. Wildcard names reported as-is with flag, NOT expanded (no guessing). Source citation: "certspotter (SSLMate CT aggregator)". crt.sh = DOWN at test time, optional config only. (g) models: need new asset kind? Kind constants in pkg/models — check existing kinds (host, api, etc.); maybe KindDomain/KindCertificate new kinds — must follow existing pattern; (h) mcp/agent discovery: classify detected assets against OWASP MCP Top 10 — check pkg/models for mcp refs already; (i) commit as v0.3.0, update ToolVersion in internal/core/config/config.go ("0.1.0"? check current — README says v0.2.1 tag; ToolVersion const may be stale "0.1.0"), README update for serve + ctlog commands/flags, push, deliver.

## STATE 8 progress update 2 (ctlog done, serve done)
DONE: internal/serve/serve.go + serve_test.go (7 tests PASS race). cmd/riskx/cmd_serve.go wired (root.AddCommand in main.go). config.go: ServeConfig{Listen,APIKey}, LLMConfig{Enabled,APIKey,Model} added to Config + defaults + validate (LLM enabled without key = input error). ResolveAPIKey: RISKX_API_KEY env wins over config. Smoke-tested: refuses start without key, 401 wrong key, 200 valid, summary works.
ctlog engine DONE: internal/discovery/ctlog/ctlog.go + ctlog_test.go (8 tests PASS race). Verified certspotter v1 URL: https://api.certspotter.com/v1/issuances?domain=%s&expand=dns_names&expand=issuer&expand=revocation — returns {id,cert_sha256,pubkey_sha256,dns_names,not_before,not_after,revoked,issuer{friendly_name,name}}. NO api key needed. Wildcards NOT expanded, co-hosted names flagged not dropped, visibility returned []string on provider failure. crt.sh DOWN (502) at verification — NOT default.
NEXT: (1) cmd: wire `riskx discover` --ct flag? OR new `riskx ct` subcommand — prefer `riskx discover --ct`? existing discover flags: --records --ports --file --data. Simplest: add DiscoverCt boolean flag to discover command calling ctlog.Discover per target, store assets+evidence via store.PutAssets/PutEvidence (store needs per-asset evidence linking: PutEvidence(findingID, assetID) — for assets without findings pass assetID, findings may be empty; check PutEvidence allows empty findingID — schema allows NULL). Also register new identity/agent/MCP cmds exist; add ct to discover output payload. (2) Phase 3 LLM layer: internal/llm/llm.go — OFF by default; env RISKX_LLM_API_KEY wins over llm.api_key config; validate: enabled without key = config error; LLM only does optional "explain" augmentation (never severity/classification/remediation); provider: OpenAI-compatible chat completions (user sets base URL via RISKX_LLM_BASE_URL or llm.base_url? keep simple: model+key only, base https://api.openai.com/v1). Evidence-only findings. Model used must be user-supplied (llm.model), never guessed default? reasonable default = "gpt-4o-mini"? NO guessing — require llm.model if enabled, or validate error. (3) README update for serve + --ct + llm section; ToolVersion bump 0.2.0?-> 0.3.0 in config.go (currently "0.1.0" stale, README already references v0.2.1 tag — bump both to 0.3.0). (4) Full test suite + race + staticcheck, commit v0.3.0, push, update tag, deliver.
README existing sections to update: commands table (add serve, ct flag), flags (add --data, RISKX_API_KEY, RISKX_LLM_API_KEY env vars), install, badges.

## STATE 8 progress update 3 (LLM layer wired)
DONE in this update:
- internal/llm/llm.go + llm_test.go (7 tests PASS race): Explain(), Config.Validate, ResolveAPIKey (env RISKX_LLM_API_KEY wins), default https://api.openai.com/v1, base_url override for self-hosted, temperature 0, max_tokens 500, 30s client timeout, fail-secure degrade with provider recorded, no request without key.
- config.go: LLMConfig{Enabled,APIKey,Model,BaseURL} + validate enabled-without-key/model = input error.
- cmd/riskx/cmd_llm.go: `riskx explain` registered in main.go. Uses cmd.Flags().GetString("text") fallback (StringVar binding was empty — fixed via debugFlag getter for text/prompt). Outputs output.Result{Meta: output.NewMeta("explain"), Payload:{native, explanation}}. findFindingByID via ListFindings. canonical() renders *models.Finding.
- Smoke tested: explain --text works with RISKX_LLM_API_KEY (needs mock server verified earlier via llm_test only; end-to-end mock server test produced no output earlier due to explain refusing — now works).
IMPORTANT quirk: cobra String flag on subcommand not binding to var in this binary (maybe cobra v1.10.2 + SilenceUsage interaction); cmd_discovery same-pattern --ports/--records DO bind via StringVar though. My explain uses GetString fallback only.
REMAINING:
1. ToolVersion in internal/core/config/config.go currently "0.1.0" — bump to "0.3.0"; also README badge tag currently v0.2.1 → update to v0.3.0.
2. README updates: commands table add `serve` and `explain` and discover --ct flag; add env vars section (RISKX_API_KEY, RISKX_LLM_API_KEY); add LLM layer section (OFF by default, user key, explanation only, no guessing).
3. Full `go test -race ./...`, go vet, staticcheck (v0.3 suite).
4. Commit "v0.3.0: serve API, CT-log discovery, LLM explain layer", push, tag v0.3.0, deliver. Note workflows still can't be pushed (no workflows perm); workflows zip already delivered previously.
5. Mock server end-to-end test optional.
