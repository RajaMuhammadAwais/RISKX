version: "2"
run:
  timeout: 10m
linters:
  default: none
  settings:
    linters:
      go:
        version: "1.24"
    govet:
      disable:
        - shadow
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unparam
    - unused
    - whitespace
